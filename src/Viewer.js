import React, { Component } from 'react';
import './App.css';

/**
 * default class, written completely in P5.js combined with a Node.JS auth server
 * gets the auth token and bearer for the user, and then uses it to make asynchronous calls to display p5 data
 */
export default class Viewer extends Component {
    render() {
        return (
            <div>test</div>
        );
    }
}